- POST_CACHE.Post.Text:
  - write <text string (BasicVariable)> from <post postnotification.Post struct (StructVariable)>
  - write <Text string (FieldVariable)> from <post postnotification.Post struct (StructVariable)>
- POST_CACHE.Post.PostID:
  - write <PostID int64 (BasicVariable)> from <post postnotification.Post struct (StructVariable)>
  - write <ref <PostID int64> @ StorageService (Reference)> from <post postnotification.Post
    struct (StructVariable)>
  - write <PostID int64 (FieldVariable)> from <post postnotification.Post struct (StructVariable)>
  - write <ref <PostID int64> @ StorageService (Reference)> from <post postnotification.Post
    struct (StructVariable)>
- POST_CACHE.Post:
  - write <post postnotification.Post struct (StructVariable)>
  - write <post postnotification.Post struct (StructVariable)> from <post postnotification.Post
    struct (StructVariable)>
  - write <ref <post postnotification.Post struct> @ StorageService (Reference)> from
    <post postnotification.Post struct (StructVariable)>
  - write <PostID int64 (FieldVariable)> from <post postnotification.Post struct (StructVariable)>
- POST_CACHE.Post.ReqID:
  - write <reqID int64 (BasicVariable)> from <post postnotification.Post struct (StructVariable)>
  - write <ReqID int64 (FieldVariable)> from <post postnotification.Post struct (StructVariable)>
- POST_CACHE.Post.Creator.Username:
  - write < "some username" string (BasicVariable)> from <post postnotification.Post
    struct (StructVariable)>
  - write <Username string (FieldVariable)> from <post postnotification.Post struct
    (StructVariable)>
- POST_CACHE.Post.Creator:
  - write <Creator postnotification.Creator struct (FieldVariable)> from <post postnotification.Post
    struct (StructVariable)>
- POST_CACHE.Post.Timestamp:
  - write <timestamp int64 (BasicVariable)> from <post postnotification.Post struct
    (StructVariable)>
  - write <Timestamp int64 (FieldVariable)> from <post postnotification.Post struct
    (StructVariable)>
