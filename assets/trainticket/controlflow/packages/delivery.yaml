(*delivery.DeliveryServiceImpl).Run(ctx context.Context) error:
  block 14 (IfDone):
  - (inline) delQ Queue
  - (inline) db NoSQLDatabase
  - d (*delivery.DeliveryServiceImpl struct{db NoSQLDatabase, delQ Queue})
  - ctx context.Context
  - delivery delivery.Delivery struct{ID string, OrderID string, FoodName string, StoreName string, StationName string}
  - didpop bool
  - err .error
  - coll NoSQLCollection {database = "delivery", collection = "delivery"}
  - err .error
  - err .error
delivery.NewDeliveryServiceImpl(ctx context.Context, queue Queue, db NoSQLDatabase)
  ((*delivery.DeliveryServiceImpl struct{db NoSQLDatabase, delQ Queue}), error):
 block 0 (Body):
  - ctx context.Context
  - queue Queue
  - db NoSQLDatabase
